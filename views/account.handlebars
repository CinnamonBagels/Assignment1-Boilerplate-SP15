<nav class="navbar navbar-inverse">
<a class="navbar-brand" href="#">
		{{#if isInstagram}}
        <img alt="Brand" src="/img/Instagram_logo.png" height="30" width="30">
		{{/if}}
		{{#if isFacebook}}
		<img src="/img/FacebookLogo.png" alt="Brand" height="30" width="30">
		{{/if}}
</a>
<a href="/logout" class="btn btn-default navbar-btn">Logout</a>

	{{#if isInstagram}}
    <p class="navbar-text">Hello, {{user.username}}</p>
	{{/if}}
	{{#if isFacebook}}
	<p class="navbar-text">Hello, {{user.displayName}}</p>
	{{/if}}
</nav>

<div class="row">
	<div class="col-lg-12">
	{{#if isFacebook}}
		<div class="row">
			<div class="col-lg-offset-3 col-lg-6"><a href="/facebooklikes" class="btn btn-info btn-block">View your Facebook <b>Likes</b></a></div>
		</div>
		<div class="row">
			<div class="col-lg-offset-3 col-lg-6"><a href="/facebookphotos" class="btn btn-info btn-block">View your Facebook <b>Photos</b></a></div>
		</div>
		<div class="row">
			<div class="col-lg-offset-3 col-lg-6"><a href="/facebookfriends" class="btn btn-info btn-block">View Your Facebook <b>Friends</b></a></div>
		</div>
	{{/if}}
		{{#if isInstagram}}
		<span>It seems you are using instagram, view your instagram images <a href="/photos">here</a>.</span>
		{{/if}}
	</div>
</div>


